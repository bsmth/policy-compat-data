{
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate:schema": "node scripts/validate-schema.mjs"
  },
  "dependencies": {
    "ajv": "^8.18.0"
  },
  "devDependencies": {
    "ajv-formats": "^3.0.1"
  }
}
